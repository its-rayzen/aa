import BookingSection from '../Booking/BookingSection';
import styles from '../../styles/BookingPage.module.scss';

export default function SPABookingSection() {
  return (
    <main className={styles.main}>
      <BookingSection />
    </main>
  );
}
